import java.util.Scanner;

public class App {
    public static int printTable() {
    /*
     * Take an input n and print a formatted multiplication table
     * @param int n from user
     * @return multiplication table from 1 to n
     */
    }
    public static String verifyNum() {
    /*
     * Detect if input n is less than 0 or greater than 50
     * @param int n from user
     * @return error message if false, allow progem to run if true
     */
    if (n )
    }
    public static void main(String[] args) throws Exception {
        Scanner scnr = new Scanner(System.in);
        System.out.println("What is the value of n?");
        int n = scnr.nextInt();
    }
}
